# Runtime output — never committed to main (SPEC.md Part 4)
projects/**

# Local secret values — must never be committed (SPEC.md Part 5)
.env
.env.local

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~
